## Запуск проекта

```bash
docker-compose up -d --build --force-recreate - поднятние docker-контейнера. Для проверки ввести адрес 127.0.0.1:85

npm install - устанавливаем зависимости
npm run dev- запуск проекта в режиме разработки
```